export interface CityType {
    id: string;
    byLine: string;
    title: string;
    shortDescription: string;
    author: string;
    createdAt: string;
    path: string;
    image?: string;
    guideId?: string;
}
